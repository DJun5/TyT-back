<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UpdateConstantSQL">
    <!-- u_info 命名为 i -->
    <sql id="dynamicInfo">
       i.id as infoId,
       i.uid as userId,
       i.title,
       i.content,
       i.praise,
       i.forward,
       i.status as dynamicStatus,
       i.browse_volume,
       i.create_time as dynamicCreateTime,
       i.update_time as dynamicUpdateTime,

       #{i.infoId},
       #{i.userId },
       #{i.title},
       #{i.content},
       #{i.praise},
       #{i.forward},
       #{i.dynamicStatus },
       #{i.browse_volume},
       #{i.dynamicCreateTime},
       #{i.dynamicUpdateTime},
    </sql>
</mapper>
