<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.djun.tyt.mapper.UserAccountMapper">

    <select id="queryAll" resultType="com.djun.tyt.model.TUser">
        SELECT
          id, uname, upassword, typeid, phone, email, uimg, ulevel, experience, health_degree, honor_degree,createTime
        FROM t_user
    </select>

    <insert id="addSubmit">
        INSERT INTO
          t_user(uname, upassword, typeid, phone, email, uimg)
        VALUES
          (#{uname},#{upassword},#{typeid},#{phone},#{email},#{uimg})
    </insert>

    <update id="update">
        update t_user set
          pwd=#{upassword},phone=#{phone},email=#{email},uimg=#{uimg}
        where id = #{id}
    </update>

</mapper>